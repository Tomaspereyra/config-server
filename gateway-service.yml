spring:
  application:
    name: gateway-service
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: http://localhost:8080/auth/realms/dh/protocol/openid-connect/certs
          issuer-uri: http://localhost:8080/auth/realms/dh    
  cloud:
    config:
      discovery:
        serviceId: config-service
        enabled: true
    gateway:
      routes:
        - id: checkout-service
          uri: lb://checkout-service
          predicates:
            - Path=/checkout
          filters:
            - AddRequestHeader=X-Request-from, gateway
            - AddResponseHeader=X-Response-Red, Blue
        - id: products-service
          uri: lb://products-service
          predicates:
            - Path=/products 
server:
  port: 8085
