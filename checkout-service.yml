spring:
  application:
    name: checkout-service
  config:
    import: configserver:http://localhost:8889
  cloud:
    config:
      discovery:
        serviceId: config-service
        enabled: true  
    
server:
  port: 8085
